import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  ScreenContainer: {
    flex: 1,
    paddingHorizontal: 30,
  },
  titleContainer: {
    paddingTop: 30,
    alignItems: 'center',
  },
  titleText: {
    fontWeight: '600',
    fontSize: 36,
    marginTop: 45,
  },
  textInpuContainer: {
    marginTop: 80,
    alignItems: 'center',
  },
});
